<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (orders.length <= 0) { %>
                <h1>Nothing there!</h1>
            <% } else { %>
                <ul>
                    <% orders.forEach(product => { %>
                        <li>
                            <h1># <%= product.dataValues.title %></h1>
                            <ul>

                                    <li><%= console.log('order id would be useful to print') %> (<%= product.dataValues.orderItem.quantity %>)</li>

                            </ul>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>